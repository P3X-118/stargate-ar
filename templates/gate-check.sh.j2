#!/bin/bash
PATH=/usr/bin:/bin:/sbin:/usr/sbin
test="$(ping -q -w10 -W2 {{ dhd_internal_ip_addr }} 2>/dev/null | grep -oP '\d{1,3}(?=%)')"
if [ "$test" == "100" ]; then
  logger -s -p user.emerg '!!! Unable to reach DHD server. Bouncing tunnel dhd0... !!!'
  systemctl restart stargate.service
fi
